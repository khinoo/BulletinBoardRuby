<h1><%= @post.title %></h1>

<p><%= @post.description %></p>

<ul>
  <li><%= link_to "Edit", edit_post_path(@post) %></li>
  <li><%= link_to "Destroy", post_path(@post),
                  method: :delete,
                  data: { confirm: "Are you sure want to this post?" } %></li>
</ul>